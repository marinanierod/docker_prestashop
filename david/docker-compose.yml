version: "3"

services:
    web:
        build: apache
        networks:
            hlc:
                ipv4_address: 172.16.2.10

networks:
    hlc:
        driver: bridge
        ipam:
            config:
            - subnet: 172.16.0.0/16
